# vendor/

Legacy code preserved for reference only.

- `legacy_vectorbdragcollector/src/` contains vendored modules copied from the old MVP / RAG collector.
- It is **not** part of the installed package set of the cleaned scaffold.
- New development should happen inside `src/mvp_sksp/`.
